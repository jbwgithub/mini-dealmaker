<div class="container d-flex flex-column mt-4">
  <h2 class="text-center mt-">
    <%= link_to "Back", templates_path, class: "me-5 btn btn-outline-secondary" %>
    <%= @template.name %>
  </h2>
</div>

<div class="container">
  <div class="row justify-content-md-center">
    <div class="col-12 mt-4">
      <div class="card text-center shadow mb-5 bg-white rounded">
        <div class="card-header fst-italic">
          by PErson
        </div>
        <div class="card-body">
          <p>This is thr template info wherr questionssould go</p>
        </div>
        <div class="card-footer text-muted">
          <small>Created <%= time_ago_in_words(@template.created_at) %> ago,
          edited <%= time_ago_in_words(@template.updated_at) %> ago </small>
        </div>
      </div>
    </div>
  </div>
</div>
