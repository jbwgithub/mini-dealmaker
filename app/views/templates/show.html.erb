<div class="container d-flex flex-column mt-4">
  <h2 class="text-center mt-">
    <%= link_to "Back", templates_path, class: "me-5 btn btn-outline-secondary" %>
    <%= @template.name %>
  </h2>
</div>

<div class="container">
  <div class="row justify-content-md-center">
    <div class="col-12 mt-4">
      <div class="card text-center shadow mb-5 bg-white rounded">
        <div class="card-header fst-italic">
          The following template is a series of investor questions for <%= @template.name %>:
        </div>
        <div class="card-body">
          <h4 class="float-left">Investor Document</h4>
          <p>INPUT vARIABLE TAGES, USE IF TO CHECK IF TEMPLATE HAS ASSIGNED QUESTIONS YET</p>
          <% if @template.questions.count > 0 %>
            <% @template.questions.each do |question| %>

              <%= question.description %></br>
            <% end %>
          <% else %>
              Substitute variable tags
          <% end %>

        </div>
        <div class="card-footer text-muted">
          <small>This template was last edited <%= time_ago_in_words(@template.updated_at) %> ago </small>
        </div>
      </div>
    </div>
  </div>
</div>
