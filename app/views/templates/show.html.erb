<div class="container d-flex flex-column mt-4">
  <h2 class="text-center mt-">
    <%= link_to "Back", templates_path, class: "me-5 btn btn-outline-secondary" %>
    <%= @template.name %>
  </h2>
</div>

<div class="container">
  <div class="row justify-content-md-center">
    <div class="col-12 mt-4">
      <div class="card border-dark text-left shadow mb-5 bg-white rounded">
        <div class="card-header text-center fst-italic">
          The following template is a series of investor questions for <%= @template.name %>:
        </div>
        <div class="card-body">
          <h4 class="text-center">Investor Document</h4>
          <p class="text-left">
            <strong>Document created on: </strong><%= @template.created_at.to_date %></br>
            <strong>Company name: </strong><%= @template.name %>
          </p>
          </br>
          <h5>Questions:</h5>
          <% if @template.questions.count > 0 %>
            <% @template.questions.each do |question| %>
              <i>Question: </i><%= question.ask %></br>
              <i>Answer: </i><%= question.answer %>
              <hr class="solid">
            <% end %>
          <% else %>
            <hr class="solid">
            <p style="color:grey"><i>Questions and answers will appear here as you assign them to this template. To create questions and assign them to templates, go to the Questions tab dropdown in the navigation bar</i></p>
            <hr class="solid">
          <% end %>

        </div>
        <div class="card-footer text-muted">
          <small>This template was last edited <%= time_ago_in_words(@template.updated_at) %> ago </small>
        </div>
      </div>
    </div>
  </div>
</div>
